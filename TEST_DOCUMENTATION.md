# Тестовая документация: `get_result`

## Классы эквивалентности

| Параметр | Класс | Ожидаемое поведение |
|---|---|---|
| `score` | меньше 0 | `"Некорректный балл"` |
| `score` | 0–49 | при любой допустимой посещаемости — не выше `"Незачёт"` |
| `score` | 50–69 | при посещаемости от 60 — `"Зачёт"` |
| `score` | 70–89 | при посещаемости от 70 — `"Хорошо"` |
| `score` | 90–100 | при посещаемости от 80 — `"Отлично"` |
| `score` | больше 100 | `"Некорректный балл"` |
| `score` | нечисловой тип | `TypeError("Баллы должны быть числом")` |
| `attendance` | меньше 0 | `"Некорректная посещаемость"` |
| `attendance` | 0–59 | не выполняет требование для `"Зачёт"` |
| `attendance` | 60–69 | подходит для `"Зачёт"` |
| `attendance` | 70–79 | подходит для `"Хорошо"` |
| `attendance` | 80–100 | подходит для `"Отлично"` |
| `attendance` | больше 100 | `"Некорректная посещаемость"` |
| `attendance` | нечисловой тип | `TypeError("Посещаемость должна быть числом")` |

## Граничные значения

| Параметр | Проверяемые значения |
|---|---|
| `score` | 0, 49, 50, 69, 70, 89, 90, 100; также -1 и 101 |
| `attendance` | 0, 59, 60, 69, 70, 79, 80, 100; также -1 и 101 |

## Покрытие тестами

`test_get_result.py` использует `pytest.mark.parametrize` для корректных классов, границ и недопустимых диапазонов. Проверки неправильных типов используют `pytest.raises()` и дополнительно сверяют текст исключения.
